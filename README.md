# Escala Trabalho

App de gestão de escala (Sesc), portado do projeto `escala` para Next.js 13 Pages Router.

## Desenvolvimento

```bash
npm install
npm run dev
```

Abre [http://localhost:3000](http://localhost:3000) (redireciona para `/dashboard`).
